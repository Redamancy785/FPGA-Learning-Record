
#include"COMMON.h"

int main(void)
{

	//初始化 PS 端 GPIO
	PS_GPIO_Init();

	//设置 PS_KEY(MIO47)方向为输入
	PS_GPIO_SetMode(PS_KEY, INPUT, 1);
	//设置 PS_KEY(MIO47)方向为输入
	PS_GPIO_SetMode(PS_LED, OUTPUT, 0);

	//GIC 通用中断控制器初始化
	ScuGic_Init();







    while(1){

    }
    return 0;
}







